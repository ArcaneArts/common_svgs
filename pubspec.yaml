name: common_svgs
description: "Common SVGs for use in Flutter projects."
version: 1.0.2
repository: https://github.com/ArcaneArts/common_svgs
environment:
  sdk: '>=3.3.4 <4.0.0'
  flutter: ">=1.17.0"
dependencies:
  flutter:
    sdk: flutter
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
flutter: